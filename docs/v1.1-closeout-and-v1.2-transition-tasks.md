# v1.1 Closeout And v1.2 Transition Tasks

Mandatory sequential P0 gate board:

`docs/v1.1-p0-sequential-gate-board.md`

All current P0 work must follow that board. A downstream specialist may start only after the previous gate has a documented and manager-accepted `PASS`.
For the current P0 flow, the sequential board supersedes the older Gate 1-4 numbering later in this document.

Date: 2026-07-10
Owner: stock-signal-scanner Manager
Status: active

## Manager Decision

We can start planning v1.2, but we cannot close v1.1 or release production yet.

Current release rule:

```text
v1.1 must pass remote signed Core E2E before production.
v1.2 planning may start in parallel.
Production release starts only after v1.1 E2E + QA + DevOps gate are green.
```

## Current Version Status

v1.1 is mostly implemented at code/test level.

Done:

- strict `/api/external/analyze` contract;
- scanner service token protection;
- Core 1.1 HMAC access/check integration;
- Core cache receipt/commit flow;
- regular and FundRep analysis/cache;
- language contract `ru/en/he`;
- user-facing `requestId` removed;
- private premium API-only boundary for `/api/external/analyze`;
- scanner does not send Telegram from contract API;
- scanner clears `chatId` from contract API flow;
- local contract smoke tests pass;
- Oleg QA passed private API-only boundary.

Current blocker:

```text
Remote signed Scanner -> Core access/check still returns HTTP 404.
```

Latest known failure:

```text
Environment: dev
requestId: devops-signed-regular-ec1f5a16-1701-4a70-8544-bc384c8e1f19
HTTP status: 503
Core access/check: failed_quota_service / Access check HTTP 404
analysis after allowed=true: not reached
cache commit status: not reached
duplicate/own_repeat result: failed closed before analysis
Telegram delivery: not sent
checked_at: 2026-07-09T18:40:57.9861224+04:00
final status: FAIL / BLOCKED BY CORE 404
```

Production status:

```text
BLOCKED
```

## Release Gates

### Gate 1 - Dev E2E

Required result:

```text
Scanner -> Core access/check -> allowed=true
Scanner runs analysis
Scanner commits cache receipt
Duplicate request returns own_repeat safely
No Telegram delivery from scanner contract API
No secrets exposed
```

Gate owner: Ilya + Core team

### Gate 2 - Scanner QA

Required result:

```text
Oleg QA PASS
No P0/P1 defects
Regression tests green
Private API-only boundary still green
Core fail-closed behavior confirmed
```

Gate owner: Oleg

### Gate 3 - Security / Integration Approval

Required result:

```text
Roman security approval
Lena integration QA approval
No private analysis leakage into broadcast channels
No secret leakage
No raw Telegram destination leakage
```

Gate owners: Roman + Lena on Core side, scanner manager monitors scanner boundary

### Gate 4 - Production Gate

Required result:

```text
Core production access/check live
Scanner production secrets configured
D1 backup exported
Migrations applied if needed
Rollback version captured
Production deploy completed
Production health checks pass
```

Gate owner: Ilya

## Tasks By Specialist

### Grisha - Scanner Developer

Priority: P0

Status: waiting for Core E2E signal; no new code unless E2E exposes scanner-side defect.

Tasks:

- [ ] Keep `/api/external/analyze` API-only for private premium flow.
- [ ] Do not re-enable Telegram delivery in contract API.
- [ ] Do not pass `chatId` to Core access/check.
- [ ] Do not use Telegram bot token from contract API.
- [ ] If Core E2E still fails after Core proves inbound request, add safe diagnostics only:
  - method;
  - URL origin;
  - URL pathname;
  - key id;
  - HTTP status;
  - short response body without secrets.
- [ ] Do not log:
  - `SERVICE_TOKEN`;
  - `CORE_HMAC_SECRET`;
  - `X-Signature`;
  - `Authorization`;
  - `X-Scanner-Token`;
  - Telegram token.
- [ ] If any scanner fix is made, run:
  - `node --check cloudflare/worker.js`;
  - `npm.cmd run test:worker-contract`.

Acceptance:

- `40/40` contract smoke tests remain green or improved.
- Scanner does not send Telegram from `/api/external/analyze`.
- Scanner analysis still requires Core allowed decision.

Report format:

```text
Task:
Status:
Changed files:
Tests:
E2E impact:
Secrets exposed: no
Risks:
Commit message:
```

### Oleg - Scanner QA

Priority: P0 after Ilya/Core E2E PASS

Tasks:

- [ ] Re-run full scanner contract regression after remote Core E2E succeeds.
- [ ] Verify `delivery.sendToTelegram=true` is rejected before access check.
- [ ] Verify `bot.tokenSecretName` is rejected before access check.
- [ ] Verify `chatId` and `telegramChatId` do not reach Core from contract API.
- [ ] Verify `Core allowed=true` runs analysis.
- [ ] Verify `Core denied` does not run analysis.
- [ ] Verify `Core unavailable/error` fails closed.
- [ ] Verify cache commit after new/refresh analysis.
- [ ] Verify duplicate `requestId + ticker` returns own_repeat safely.
- [ ] Verify no provider/Telegram repeat on duplicate.
- [ ] Verify no raw internal fields are shown to user-facing report text.
- [ ] Verify no secrets in response/log samples.

Acceptance:

- QA verdict `PASS`.
- No P0/P1 defects.
- If defects exist, classify and return to Grisha/Ilya/Core owner.

Report format:

```text
Task:
Environment:
Scanner version:
Tests:
E2E scenarios:
Private API-only boundary:
Core fail-closed:
Cache commit:
Duplicate/own_repeat:
Secrets exposed: no
Result: PASS/FAIL
Blockers:
```

### Ilya - Scanner DevOps / Cloudflare

Priority: P0

Tasks:

- [ ] Coordinate live E2E window with Core team.
- [ ] Confirm scanner dev version before E2E.
- [ ] Confirm safe bindings only:
  - `APP_ENV`;
  - `ACCESS_CHECK_URL`;
  - `CORE_HMAC_KEY_ID`.
- [ ] Confirm secret names exist without printing values:
  - `SERVICE_TOKEN`;
  - `CORE_HMAC_SECRET`.
- [ ] Repeat signed remote E2E with new `requestId`.
- [ ] Capture safe response metadata:
  - status;
  - response body without secrets;
  - timestamp;
  - optional `cf-ray` if available.
- [ ] Verify:
  - access/check reached Core;
  - analysis ran only after allowed;
  - cache commit reached Core;
  - duplicate/own_repeat works;
  - Telegram not sent.
- [ ] If E2E passes, prepare production gate checklist.
- [ ] If E2E fails, state exact failing boundary and next owner.

Do not expose:

- secret values;
- HMAC signatures;
- raw auth headers;
- Telegram tokens;
- raw service tokens.

Report format:

```text
Environment:
Scanner version:
Core URL:
requestId:
Access check status:
Analysis status:
Cache commit status:
Duplicate/own_repeat:
Telegram delivery:
Secrets exposed: no
Result: PASS/FAIL
Blockers:
Next owner:
```

### Masha - UX / Localization

Priority: P1 after E2E PASS

Tasks:

- [ ] Review user-facing wording for private premium analysis states:
  - access denied;
  - no quota;
  - temporary unavailable;
  - cached report;
  - own recent report;
  - force refresh.
- [ ] Confirm wording does not expose:
  - Core;
  - HMAC;
  - nonce;
  - cache receipt;
  - quotaDecision;
  - requestId;
  - internal failure details.
- [ ] Confirm RU/EN wording for private response is separate from broadcast channel wording.
- [ ] Add HE wording only if Core/private bot requires Hebrew private responses in v1.1.

Acceptance:

- User copy is safe, non-technical, and does not imply personalized investment advice.
- Copy is ready for Core/private bot/site implementation.

Report format:

```text
Task:
Status:
Updated keys/text:
Languages:
Forbidden terms checked:
Risks:
Result:
```

### Anna - Product / Quota Economics

Priority: P1 before production pricing copy freezes

Tasks:

- [ ] Reconfirm v1.1 quota model after private premium boundary:
  - regular new;
  - regular cached;
  - regular refresh;
  - FundRep new;
  - FundRep cached;
  - own repeat.
- [ ] Confirm whether decimal units remain acceptable for first release.
- [ ] Define v1.2 decision trigger for moving to whole credits.
- [ ] Define analytics needed from Core:
  - report type;
  - cache status;
  - charge units;
  - no quota;
  - no access;
  - own repeat;
  - refresh;
  - user complaints/support tags.

Acceptance:

- v1.1 pricing remains approved or updated.
- v1.2 product experiment list is ready.

Report format:

```text
Task:
Status:
Recommendation:
Risks:
v1.1 decision:
v1.2 follow-up:
```

### Core Team / market-signal-ai-bot

Priority: P0

Tasks:

- [ ] Investigate why real signed Scanner request gets `HTTP 404`.
- [ ] Confirm whether request reaches Core Worker execution.
- [ ] If request does not reach Worker, identify Cloudflare route/rule/deployment reason.
- [ ] If request reaches Worker, identify handler/path/HMAC reason.
- [ ] Confirm both paths return `401` without HMAC:
  - `/api/internal/access/check`;
  - `/api/internal/access/check/`.
- [ ] Confirm key id `scanner-dev-v2` is active.
- [ ] Confirm signed request path expected by HMAC canonical string.
- [ ] Run live tail during Scanner E2E.
- [ ] Return safe report without secrets.

Acceptance:

- Scanner signed E2E no longer receives `404`.
- Core returns a valid access decision.
- Core records quota/access/audit decision.

Report format:

```text
Environment:
Core version:
Scanner requestId:
Request reached Worker: yes/no
Matched route:
HMAC status:
Access decision:
HTTP status:
Secrets exposed: no
Result:
Blockers:
```

### telegram-company-matcher Manager

Priority: P2 / monitor

Tasks:

- [ ] Keep matcher focused on broadcast news delivery only.
- [ ] Do not send private ticker requests through matcher.
- [ ] Do not send raw chat IDs to scanner.
- [ ] Do not send Telegram bot tokens to scanner.
- [ ] Keep broadcast route evidence available for Lena/Roman.

Acceptance:

- Matcher does not participate in private premium analysis except where Core explicitly defines a delivery contract.

### Claude / Website

Priority: P2 after Core private flow is stable

Tasks:

- [ ] Call scanner only through backend/Worker, never browser direct.
- [ ] Keep `STOCK_SIGNAL_SCANNER_TOKEN` in Worker secrets only.
- [ ] Use Core/private API flow for premium analysis.
- [ ] Show scanner response as analysis content only, not raw internal JSON.
- [ ] Do not show:
  - `requestId`;
  - `quotaDecision`;
  - `cacheReceiptId`;
  - internal error codes;
  - HMAC/Core details.

Acceptance:

- Browser never sees scanner service token.
- User sees clear private analysis result and safe failure messages.

## v1.1 Closeout Checklist

- [ ] Core signed E2E PASS.
- [ ] Scanner analysis after `allowed=true` PASS.
- [ ] Cache commit PASS.
- [ ] Duplicate/own_repeat PASS.
- [ ] Private API-only QA PASS.
- [ ] Full scanner QA PASS.
- [ ] Core security review PASS or PASS WITH ACCEPTED RISKS.
- [ ] Integration QA PASS or READY WITH ACCEPTED RISKS.
- [ ] Production gate prepared.
- [ ] Release notes ready.

## v1.2 Planning Backlog

These can be planned now, but should not block v1.1 production unless a P0 is found.

- [ ] Decide decimal units vs whole credits.
- [ ] Improve report UI formatting for website/private bot.
- [ ] Add analytics dashboard for report type/cache/charge/failures.
- [ ] Decide legacy Telegram scanner paths:
  - dev only;
  - separate service;
  - remove after Core private bot is stable.
- [ ] Clean or document legacy subscription tables in scanner schema.
- [ ] Add richer FundRep presentation for private web results.
- [ ] Add structured non-technical failure messages per language.

## Production Release Plan

Production release starts only after v1.1 closeout checklist is green.

Steps:

1. Capture current production Worker version.
2. Export production D1 backup.
3. Confirm Core production access/check and cache/commit are live.
4. Configure scanner production secrets through protected flow.
5. Deploy scanner production.
6. Run production health checks.
7. Run production smoke:
   - invalid token;
   - valid token;
   - Core allowed;
   - Core denied;
   - Core unavailable fail-closed;
   - regular analysis;
   - FundRep;
   - cache commit;
   - duplicate/own_repeat;
   - no Telegram from contract API.
8. Confirm rollback command and version.
9. Publish release notes.

## Current Manager Verdict

```text
Do not release production yet.
Finish v1.1 E2E.
Start v1.2 planning in parallel.
Next blocking owner: Core team + Ilya.
```

## Grisha Report - 2026-07-10

```text
Task:
SCANNER-P0-PRIVATE-API-ONLY-BOUNDARY verification for v1.1 closeout and v1.2 transition.

Status:
DONE / SCANNER SIDE VERIFIED

Changed files:
- cloudflare/worker.js
- tests/worker-contract-smoke.mjs
- docs/v1.1-closeout-and-v1.2-transition-tasks.md

Tests:
- node --check cloudflare/worker.js: PASS
- npm.cmd run test:worker-contract: PASS
- contract smoke runner output: all listed tests passed

Verified:
- /api/external/analyze remains API-only for private premium flow.
- delivery.sendToTelegram=true is rejected before Core access/check.
- bot.tokenSecretName is rejected before Core access/check.
- contract API normalizes chatId to null.
- contract API does not pass chatId to Core access/check.
- contract API does not call Telegram fetch.
- contract API does not use TELEGRAM_BOT_TOKEN.
- legacy Telegram paths remain separate from /api/external/analyze.
- Core allowed decision is still required before analysis.
- Core denied/error remains fail-closed.

E2E impact:
Scanner-side boundary is ready for the next remote signed E2E.
Known remote blocker remains Core access/check HTTP 404 and is owned by Core team + Ilya.
No production release is allowed until remote signed E2E, QA and DevOps gates pass.

Secrets exposed: no

Risks:
- Current task did not attempt remote E2E because the document says Scanner is waiting for Core E2E signal.
- The local smoke output passes all listed tests; if release tracking requires the exact label 40/40, QA should align the count with the current test runner list.

Commit message:
fix(worker): enforce private premium API-only boundary
```

## Grisha Report - 2026-07-13

```text
Task:
Scanner CORE_SERVICE binding transport for Core access/check and access/cache/commit.

Status:
DONE / READY FOR OLEG QA AND ILYA DEVOPS CHECK

Changed files:
- cloudflare/worker.js
- tests/worker-contract-smoke.mjs
- wrangler.worker.toml
- CLOUDFLARE_DEPLOY.md
- docs/v1.1-closeout-and-v1.2-transition-tasks.md

Implementation:
- Scanner now resolves Core transport through env.CORE_SERVICE.
- access/check uses env.CORE_SERVICE.fetch(...).
- access/cache/commit uses env.CORE_SERVICE.fetch(...).
- public fetch() fallback for Core is removed for dev and production.
- Missing CORE_SERVICE fails closed before analysis.
- HMAC headers, canonical pathname, canonical query and body hash are preserved.
- ACCESS_CHECK_URL/MARKET_SIGNAL_AI_BOT_URL are no longer transport fallback; they are only optional canonical path configuration.
- wrangler.worker.toml currently includes the dev CORE_SERVICE binding only.
- Production CORE_SERVICE binding is intentionally not added yet; bindings are not inherited between environments and Ilya must add it after confirming the real Core production Worker name.

Tests:
- node --check cloudflare/worker.js: PASS
- node --check tests/worker-contract-smoke.mjs: PASS
- npm.cmd run test:worker-contract: PASS

New/updated test coverage:
- access/check is called through CORE_SERVICE binding.
- access/cache/commit is called through CORE_SERVICE binding.
- missing CORE_SERVICE returns fail-closed before provider, cache commit or Telegram.
- Core deny/error does not run provider, cache commit or Telegram.
- duplicate request still calls Core through binding, but does not repeat provider analysis or cache commit.
- no public fetch fallback is allowed for Core mock endpoints.

E2E impact:
The previous Core HTTP 404 public-route blocker should be bypassed by service binding transport.
Ilya should verify the dev Worker has a CORE_SERVICE binding to the correct Core Worker and rerun remote signed E2E.
Oleg should rerun scanner contract regression and private API-only boundary checks after Ilya confirms binding deployment.

Secrets exposed: no

Risks:
- Production binding is currently absent by design and must be added before production deploy after the actual Core production Worker name is confirmed.
- If CORE_SERVICE is missing in any environment, contract API correctly fails closed with HTTP 503 before analysis.

Commit message:
feat(worker): route Core access through service binding
```

## Grisha Report - 2026-07-13 Encoding Fix

```text
Task:
Fix corrupted user-facing encoding in worker.js and correct production binding report.

Status:
DONE

Changed files:
- cloudflare/worker.js
- tests/worker-contract-smoke.mjs
- CLOUDFLARE_DEPLOY.md
- docs/v1.1-closeout-and-v1.2-transition-tasks.md

Fixed:
- Russian Telegram prompts and auth/error messages no longer contain mojibake.
- Telegram report symbols and separators are restored.
- Market-data user errors are restored.
- FundRep/PromtRep Telegram-facing symbols are restored.
- Existing Service Binding changes were preserved.
- Production CORE_SERVICE binding is documented as intentionally absent for now.

Production binding note:
wrangler.worker.toml currently contains only the dev CORE_SERVICE binding.
Production binding is not inherited and must be added separately by Ilya after confirming the real Core production Worker name.

Tests:
- node --check cloudflare/worker.js: PASS
- node --check tests/worker-contract-smoke.mjs: PASS
- npm.cmd run test:worker-contract: PASS
- git diff --check: PASS

New test coverage:
- user-facing Telegram messages are checked for typical mojibake markers.
- localized JSON report text is checked for mojibake markers.

Secrets exposed: no

Commit message:
fix(worker): restore user-facing text encoding
```
