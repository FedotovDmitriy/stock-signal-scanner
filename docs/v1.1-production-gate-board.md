# v1.1 Production Gate Board

Updated: 2026-07-15

## Current Decision

```text
Status: BLOCKED / DO NOT DEPLOY PRODUCTION SCANNER
Reason: production Core and production Scanner infrastructure are not both ready for the v1.1 access/cache flow.
Production changed by this board: no
Secrets exposed: no
```

This board starts after the accepted v1.1 dev closeout in `docs/v1.1-p0-sequential-gate-board.md`.

Production must move only by the gates below. If any gate is `FAIL` or `BLOCKED`, the next gate does not start.

## Confirmed Inputs

### From Grisha

```text
Scanner code/config review:
- Public Core fallback is not restored.
- Scanner calls Core through env.CORE_SERVICE.fetch(...).
- Without CORE_SERVICE, Scanner fails closed.
- Contract API remains API-only.
- delivery.sendToTelegram=true is rejected.
- bot.tokenSecretName is rejected.
- /api/external/analyze does not send Telegram.
- Telegram remains only in legacy/dev paths.
- Local checks passed.
```

Important update: current `wrangler.worker.toml` already contains production non-secret config added by Ilya:

```text
ACCESS_CHECK_URL=https://market-signal-ai-bot.fnemoy.workers.dev/api/internal/access/check
CORE_HMAC_KEY_ID=scanner-prod-v1
CORE_SERVICE -> market-signal-ai-bot
```

Grisha must review the current diff again before production deploy because his earlier report was created before this production config appeared.

### From Core Team

```text
Production Core Worker name: market-signal-ai-bot
Production URL: https://market-signal-ai-bot.fnemoy.workers.dev
Active production version: 967192f3-12cc-4176-8dc5-ceb0b0d21ae7
Health: 200, ok=true
Production changed: no
Secrets exposed: no
```

Production Core blockers:

```text
Production HMAC key id: not configured / not confirmed
POST /api/internal/access/check -> 404
POST /api/internal/access/cache/commit -> 404
Pending migrations: 0007-0016
Missing tables/fields:
- quota_decisions_v11
- cache_receipts
- core_cache_entries
- cache_entry_id
- cache_receipt_id
```

### From Ilya

```text
Production deploy: not performed
Production Core Worker confirmed: market-signal-ai-bot
Core production active version: 967192f3-12cc-4176-8dc5-ceb0b0d21ae7
Scanner rollback candidate: d73ccba6-fe20-4475-8df4-3fc5df2cde5a
Scanner production D1 backup created:
backups/stock_signal_scanner_production-20260715-203425.sql
Production dry-run: PASS, planned CORE_SERVICE target market-signal-ai-bot
Repo secret scan: no tokens/secrets found
```

Ilya blockers:

```text
[P0] Scanner production secret CORE_HMAC_SECRET is missing.
[P0] Scanner production D1 schema is incomplete.
[P0] Core production access/check is not live.
[P1] Active Scanner production has suspicious plain_text binding name TELEGRAM_BOT_TOKEN with trailing space.
```

### From Oleg

```text
Production smoke: FAIL / BLOCKED
/api/status: PASS
Regular production analysis: BLOCKED
Exact duplicate / own repeat / shared cache: BLOCKED
sendToTelegram=false authenticated smoke: BLOCKED
Public response secret scan: PASS
request_logs secret scan: PASS
```

Oleg blockers:

```text
Active Scanner production version is old.
Active Scanner production does not have CORE_SERVICE.
Active Scanner production does not have ACCESS_CHECK_URL / CORE_HMAC_KEY_ID.
Suspicious plain_text Telegram-token-like binding with trailing space exists in active production version.
```

## Gate Status

| Order | Gate | Owner | Status | May start |
|---|---|---|---|---|
| 0 | Production stop record | Scanner manager | PASS | Completed |
| 1 | Core production readiness | Core team | ACTIVE | Now |
| 2 | Scanner production config and secret hygiene | Grisha + Ilya | LOCKED | Only after Gate 1 PASS |
| 3 | Scanner production D1 and deploy gate | Ilya | LOCKED | Only after Gate 2 PASS |
| 4 | Production smoke and regression QA | Oleg | LOCKED | Only after Gate 3 PASS |
| 5 | Manager production release decision | Scanner manager | LOCKED | Only after Gate 4 PASS |

## Gate 0 - Production Stop Record

Owner: Scanner manager

Status: PASS

Decision:

```text
Production Scanner deploy is blocked now.
Production release can continue only through this board.
No one may deploy production Scanner until Gates 1-3 are PASS.
No one may close production release until Gate 4 is PASS and Gate 5 is accepted.
```

## Gate 1 - Core Production Readiness

Owner: Core team / market-signal-ai-bot manager

Status: ACTIVE

Goal: make production Core ready for Scanner v1.1 HMAC access/cache flow.

Tasks:

1. Create Core production D1 backup before migrations.
2. Apply Core production migrations `0007-0016`.
3. Verify required production Core tables/fields:
   - `quota_decisions_v11`;
   - `cache_receipts`;
   - `core_cache_entries`;
   - `cache_entry_id`;
   - `cache_receipt_id`.
4. Configure production HMAC key id and secret map for Scanner:
   - expected key id: `scanner-prod-v1`, unless Core team gives a different final id;
   - do not print secret values.
5. Deploy Core production version that contains:
   - `POST /api/internal/access/check`;
   - `POST /api/internal/access/cache/commit`;
   - ownership logic for exact duplicate, own_repeat, and cached_regular.
6. Verify public unsigned endpoint behavior:
   - `GET /api/health` -> `200`;
   - unsigned `POST /api/internal/access/check` -> auth/HMAC reject, not `404`;
   - unsigned `POST /api/internal/access/cache/commit` -> auth/HMAC reject, not `404`.
7. Confirm rollback target and rollback command.
8. Report only safe data.

Acceptance criteria:

```text
Core production health: PASS
Core production migrations: PASS
Core production HMAC key id: configured yes
access/check route: live, not 404
cache/commit route: live, not 404
Exact duplicate / own_repeat / cached_regular logic: present in production build
Secrets exposed: no
Production Core rollback plan: present
Result: PASS
```

Report format:

```text
Task ID: CORE-PROD-V1.1-ACCESS-CACHE-READINESS
Environment: production
Core Worker name:
Core version:
Backup created: yes/no
Migrations 0007-0016: PASS/FAIL
Required tables/fields: PASS/FAIL
HMAC key id configured: yes/no
GET /api/health:
POST /api/internal/access/check unsigned:
POST /api/internal/access/cache/commit unsigned:
Rollback target:
Rollback command:
Secrets exposed: no
Production changed: yes/no
Result: PASS/FAIL
Blockers:
```

Gate 2 remains locked unless Gate 1 says `Result: PASS`.

## Gate 2 - Scanner Production Config And Secret Hygiene

Owners: Grisha + Ilya

Status: LOCKED

Start condition: Gate 1 is `PASS`.

Goal: prepare Scanner production configuration without deploying production Scanner.

Tasks for Grisha:

1. Re-review current `wrangler.worker.toml` after Ilya's production config changes.
2. Confirm production `CORE_SERVICE -> market-signal-ai-bot`.
3. Confirm production `ACCESS_CHECK_URL` and `CORE_HMAC_KEY_ID` are under `env.production.vars`.
4. Confirm no public Core fetch fallback exists.
5. Confirm contract API is still API-only:
   - rejects `delivery.sendToTelegram=true`;
   - rejects `bot.tokenSecretName`;
   - does not send Telegram in `/api/external/analyze`.
6. Run local regression.

Tasks for Ilya:

1. Configure Scanner production `CORE_HMAC_SECRET` securely.
2. Verify required Scanner production secrets by name only:
   - `SERVICE_TOKEN`;
   - `CORE_HMAC_SECRET`;
   - any existing legacy secrets required by legacy endpoints.
3. Remove or neutralize the suspicious production plain_text binding named like `TELEGRAM_BOT_TOKEN ` with trailing space.
4. Confirm no secret values are printed, committed, logged, or placed in this board.
5. Run production dry-run again and confirm planned bindings.

Acceptance criteria:

```text
wrangler.worker.toml production config: PASS
CORE_SERVICE target: market-signal-ai-bot
CORE_HMAC_SECRET configured: yes
Suspicious plain_text Telegram binding removed/neutralized: yes
Production dry-run: PASS
Production deploy performed: no
Secrets exposed: no
Result: PASS
```

Report format:

```text
Task ID: SCANNER-PROD-V1.1-CONFIG-SECRET-HYGIENE
Environment: production
Grisha code/config review: PASS/FAIL
Ilya secrets/bindings review: PASS/FAIL
CORE_SERVICE target:
CORE_HMAC_KEY_ID:
CORE_HMAC_SECRET configured: yes/no
SERVICE_TOKEN configured: yes/no
Suspicious plain_text binding status:
Dry-run result:
Local regression:
Production deploy performed: no
Secrets exposed: no
Result: PASS/FAIL
Blockers:
```

Gate 3 remains locked unless Gate 2 says `Result: PASS`.

## Gate 3 - Scanner Production D1 And Deploy Gate

Owner: Ilya

Status: LOCKED

Start condition: Gate 2 is `PASS`.

Goal: safely migrate and deploy Scanner production after Core production is ready.

Tasks:

1. Create a fresh Scanner production D1 backup, even though a previous backup exists.
2. Verify or apply Scanner production schema/migrations for:
   - `contract_results`;
   - `analysis_tasks`;
   - `analysis_cache`;
   - `request_logs`;
   - `ticker_request_logs`.
3. Confirm rollback candidate before deploy.
4. Deploy accepted v1.1 Scanner to production.
5. Confirm active production Scanner version.
6. Confirm active production bindings:
   - `CORE_SERVICE -> market-signal-ai-bot`;
   - `CORE_HMAC_KEY_ID=scanner-prod-v1`;
   - production D1 binding points to `stock_signal_scanner_production`.
7. Run minimal production health:
   - `GET /api/status`;
   - unauthenticated `POST /api/external/analyze` -> `403`.
8. Do not run full business smoke here; that belongs to Oleg in Gate 4.

Acceptance criteria:

```text
Backup: PASS
Scanner production schema: PASS
Deploy: PASS
Active version confirmed: PASS
Bindings confirmed: PASS
Minimal health: PASS
Rollback plan: PASS
Secrets exposed: no
Result: PASS
```

Report format:

```text
Task ID: SCANNER-PROD-V1.1-DEPLOY-GATE
Environment: production
Backup file:
Scanner schema/tables: PASS/FAIL
Rollback candidate:
Rollback command:
Production deploy performed: yes/no
Scanner production version:
CORE_SERVICE target:
CORE_HMAC_KEY_ID:
GET /api/status:
Unauth external analyze:
Secrets exposed: no
Result: PASS/FAIL
Blockers:
```

Gate 4 remains locked unless Gate 3 says `Result: PASS`.

## Gate 4 - Production Smoke And Regression QA

Owner: Oleg

Status: LOCKED

Start condition: Gate 3 is `PASS`.

Goal: independently verify production behavior after deployment.

Tasks:

1. Verify active Scanner and Core production versions.
2. Verify `GET /api/status`.
3. Run authenticated regular analysis through production Core with protected local `SERVICE_TOKEN`.
4. Verify exact duplicate returns saved original response and does not repeat provider/commit.
5. Verify same-user own repeat:
   - `quotaDecision=own_repeat`;
   - `chargeUnits=0`;
   - no new receipt/commit;
   - cache hit.
6. Verify other-user shared cache:
   - `quotaDecision=cached_regular`;
   - cached price;
   - no provider repeat.
7. Verify `delivery.sendToTelegram=false` stays not delivered.
8. Verify private API rejects Telegram delivery fields.
9. Verify fail-closed behavior using safe local/regression evidence if remote mutation would be risky.
10. Scan public responses and D1 logs for secret markers without exposing values.

Acceptance criteria:

```text
Production regular analysis: PASS
Exact duplicate: PASS
Own repeat: PASS
Shared cache: PASS
Telegram not sent: PASS
Secrets exposed: no
Production smoke result: PASS
Ready for manager decision: yes
```

Report format:

```text
Task ID: SCANNER-PROD-V1.1-FINAL-SMOKE-QA
Environment: production
Scanner version:
Core version:
Regular analysis:
Exact duplicate:
Own repeat:
Shared cache:
Telegram delivery:
Private API boundary:
Fail-closed:
Secret scan:
Residual risks:
Secrets exposed: no
Result: PASS/FAIL
Ready for manager decision: yes/no
Blockers:
```

Gate 5 remains locked unless Gate 4 says `Result: PASS` and `Ready for manager decision: yes`.

## Gate 5 - Manager Production Release Decision

Owner: Scanner manager

Status: LOCKED

The manager may accept production release only when Gates 1-4 are all `PASS`, reports are present, production behavior is verified, and no P0/P1 blocker remains.

Decision template:

```text
Decision: ACCEPTED/REJECTED
Gate 1 Core readiness: PASS/FAIL
Gate 2 Scanner config/secrets: PASS/FAIL
Gate 3 Scanner deploy gate: PASS/FAIL
Gate 4 production smoke QA: PASS/FAIL
Open blockers:
Rollback plan confirmed: yes/no
Secrets exposed: no
Decision date:
Comment:
```
